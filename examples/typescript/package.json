{
  "scripts": {
    "dev": "NODE_ENV=development nodemon --exec ts-node dev.ts --project ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@vercel/node": "2.9.9",
    "nodemon": "2.0.20",
    "ts-node": "10.9.1",
    "typescript": "4.9.5",
    "vercel-dev-server": "file:../.."
  }
}
